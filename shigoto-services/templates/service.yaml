apiVersion: v1
kind: Service
metadata:
  name: {{ include "shigoto-services.fullname" . }}
  labels:
    {{- include "shigoto-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shigoto-services.selectorLabels" . | nindent 4 }}
